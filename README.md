# AV1-WEB-MARCELLE-08-05

This repository contains the AV1 web project with `backend` and `frontend` folders.

- Backend: Node/Express with Prisma
- Frontend: Vite + React

Created and initialized locally. Remote not added per request.
